# This is a placeholder file.
# Students will run `dvc init`, `dvc add data/data.csv`, and `dvc repro`.
stages:
  train:
    cmd: python ml/train.py
    deps:
      - data/data.csv
      - ml/train.py
      - ml/params.yml
    outs:
      - api/model.pkl
